#include <stdio.h>

/**
 * main - prints the exact text followed by a new line
 *
 * Return: always 0 when successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
